[build]
  command = "rm -rf dist && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback so client-side routes like /admin don't 404 on first load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
